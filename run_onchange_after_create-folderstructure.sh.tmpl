#!/bin/sh

# Folder structure is defined in .chezmoidata/folderstructure.yaml {{ include ".chezmoidata/folderstructure.yaml" | sha256sum }}

# Create dev folder structure
echo "Creating dev folder structure..."
{{ range .folderstructure.dev -}}
mkdir -p "{{ $.chezmoi.homeDir }}/dev/{{ . }}"
{{ end -}}
